{
  "name": "@math1400-ignite-ui/global",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@math1400-ignite-ui/docs && changeset publish"
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  },
  "packageManager": "npm@10.7.0",
  "dependencies": {
    "@changesets/cli": "^2.27.7",
    "@radix-ui/react-checkbox": "^1.1.1",
    "@radix-ui/react-tooltip": "^1.1.2"
  }
}
